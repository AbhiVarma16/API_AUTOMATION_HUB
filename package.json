{
  "name": "bookstore-api-tests",
  "version": "1.0.0",
  "description": "npm install ## Run tests npx jest ## Configuration Endpoints configured via /utils/config.js",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:clean": "jest --silent=false --noStackTrace"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.1.0",
    "jest": "^30.0.5",
    "jest-html-reporter": "^4.3.0",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  }
}
